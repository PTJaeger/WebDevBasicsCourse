Restructured image gallery and text boxes on landing page
Improved responsiveness on mobile views, catalog video no longer gets cut off when viewed on a phone, gallery images become scrollable on when viewport is sufficiently narrow, navbar becomes vertical when on mobile.
Added JS input validation for phonenumber, name and email on order form. Loosened regex rules from task-h to allow for numbers fitting the pattern of +2-4 digits-2-4 digits-2-4 digits-2-4 digits. Missing is still date input validation to eliminate cases where the start date is after the end date.
Added link functionality to catalog page item buttons, they direct to the ordering page.
Slight styling alterations for better consistency eq rounded corners and shadowing on all box elements.
